@model List<Models.EntityFramework.ProductCategory>
<div class="categories">
    <h3>Categories</h3>
    <ul>
        @foreach (var item in Model)
        {
            <li><a href="/san-pham/@item.MetaTitle-@item.ID">@item.Name</a></li>
        }
    </ul>
</div>
